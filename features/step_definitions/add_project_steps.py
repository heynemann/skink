# -*- coding: utf-8 -*-
from lettuce import step

@step(u'Given I am logged in')
def given_i_am_logged_in(step):
    assert False, 'This step must be implemented'

@step(u'And I go to new project page')
def and_i_go_to_new_project_page(step):
    assert False, 'This step must be implemented'

@step(u'When I submit the form with the following data:')
def when_i_submit_the_form_with_the_following_data(step):
    assert False, 'This step must be implemented'

@step(u'Then I should be redirected to the projects list page')
def then_i_should_be_redirected_to_the_projects_list_page(step):
    assert False, 'This step must be implemented'

@step(u'And it should contains the projects saved above')
def and_it_should_contains_the_projects_saved_above(step):
    assert False, 'This step must be implemented'
